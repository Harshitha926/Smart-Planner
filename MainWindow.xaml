<Window x:Class="SmartPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Smart Planner"
        Height="740"
        Width="1120"
        WindowStartupLocation="CenterScreen"
        Background="#FFF2F4F7">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Background" Value="#FF3B82F6" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NavigationButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#FF4F46E5" />
            <Setter Property="Width" Value="160" />
        </Style>
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="18" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Padding" Value="22" />
            <Setter Property="Margin" Value="0,0,0,20" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#33000000" BlurRadius="18" ShadowDepth="4" Opacity="0.12" />
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <DockPanel LastChildFill="True">
        <Border DockPanel.Dock="Top" Background="White" Padding="20" CornerRadius="0,0,20,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource NavigationButton}" Content="Task Input" Click="TaskInputButton_Click" />
                    <Button Style="{StaticResource NavigationButton}" Content="Visual Schedule" Click="VisualScheduleButton_Click" />
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="Smart Planner" FontSize="28" FontFamily="Segoe UI Semibold" Foreground="#FF111827" />
                    <TextBlock Text="Manage tasks, build a daily schedule, and get live reminders." FontSize="14" Foreground="#FF6B7280" />
                </StackPanel>
                <Border Grid.Column="2" Style="{StaticResource CardBorder}" Background="#FFF8FAFF" Width="180" HorizontalAlignment="Right">
                    <StackPanel>
                        <TextBlock Text="Active Tasks" FontSize="12" Foreground="#FF6B7280" />
                        <TextBlock Text="{Binding TaskManager.Tasks.Count}" FontSize="24" FontWeight="Bold" Foreground="#FF111827" Margin="0,8,0,0" />
                        <TextBlock Text="Total tasks in the list" FontSize="12" Foreground="#FF9CA3AF" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
        <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" Background="#FFF2F4F7" />
    </DockPanel>
</Window>
